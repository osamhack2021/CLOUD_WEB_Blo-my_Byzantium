export interface FirearmListElement {
  status: string;
  fireArmNumber: string;
  owner: string;
  reason: string;
  time: string;
}

export type foodDataSearchType = {
  corps: string;
  food: string;
};
